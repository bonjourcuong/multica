"use client";

/**
 * Stub — replaced in T11. Lets `global-chat-view.tsx` typecheck while the
 * package is being scaffolded incrementally.
 */
export function WorkspaceTilesGrid() {
  return (
    <div className="flex flex-1 rounded-md border bg-card text-card-foreground" />
  );
}
