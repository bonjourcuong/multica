"use client";

/**
 * Stub — replaced in T13. Lets `global-chat-view.tsx` typecheck while the
 * package is being scaffolded incrementally.
 */
export function GlobalChatPane() {
  return (
    <div className="flex h-full flex-col rounded-md border bg-card text-card-foreground" />
  );
}
